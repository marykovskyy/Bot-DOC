# Верхні межі (<N+1) захищають від несумісних мажорних оновлень при
# переустановці. Перевірено на Python 3.12 (2026-07-07): всі версії сумісні.

# ── Telegram Bot framework ────────────────────────────────────────────────
python-telegram-bot==20.7

# ── HTTP та .env ──────────────────────────────────────────────────────────
requests>=2.31.0,<3
python-dotenv>=1.0.0,<2
aiohttp>=3.9.0,<4

# ── Планувальник ──────────────────────────────────────────────────────────
apscheduler>=3.10.4,<4

# ── Google API (Sheets + Drive) ───────────────────────────────────────────
google-api-python-client>=2.100.0,<3
google-auth>=2.23.0,<3
google-auth-oauthlib>=1.1.0,<2
gspread>=5.12.0,<7

# ── Браузер-автоматизація (scrapers) ──────────────────────────────────────
DrissionPage>=4.0.4,<5

# ── AWS S3 + Textract (AI-sorter) ─────────────────────────────────────────
boto3>=1.34.0,<2

# ── OpenAI (AI-sorter) ────────────────────────────────────────────────────
openai>=1.12.0,<3

# ── Дані та зображення ────────────────────────────────────────────────────
pandas>=2.1.0,<4
Pillow>=10.2.0,<13
numpy>=1.26,<3
opencv-python>=4.9,<6
scikit-image>=0.22,<1
openpyxl>=3.1,<4

# ── OCR (RapidOCR + Tesseract; потрібен встановлений Tesseract-OCR у системі)
rapidocr-onnxruntime>=1.4.0,<2
pytesseract>=0.3.10,<1
psd-tools>=1.9.31,<2

# ── Крипто (scrapers/turkey.py) ───────────────────────────────────────────
pycryptodome>=3.20.0,<4

# ── Моніторинг помилок (опційно) ──────────────────────────────────────────
sentry-sdk>=2.0.0,<3

# ── Утиліти ───────────────────────────────────────────────────────────────
python-dateutil>=2.8.2,<3
